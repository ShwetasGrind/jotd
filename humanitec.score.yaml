apiVersion: humanitec.org/v1b1
profile: "humanitec/default-module"
spec:
  "ingress":
    rules:
      "${resources.jotdcontainerid-dns}":
        http:
          "/":
            type: prefix
            port: 3000
